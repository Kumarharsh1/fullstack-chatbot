[build]
  base = "client"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# Redirect all requests to index.html (for React Router)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
