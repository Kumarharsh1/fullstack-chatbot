[build]
  base = "client"
  command = "npm run build"
  publish = "build"

[build.environment]
  NODE_VERSION = "18"

# For client-side routing in React (Single Page Application)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
